import { HonorificPipePipe } from './honorific-pipe.pipe';

describe('HonorificPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HonorificPipePipe();
    expect(pipe).toBeTruthy();
  });
});
